<%= @tweets.each do |tweet| %>
  <% unless tweet.media.blank? %>
    <%= render 'tweet', image_source: tweet.media[0].media_url,
                        creation_time: tweet.created_at,
                        twitter_user_name: tweet.user.name %>
  <% end %>
<% end  %>
