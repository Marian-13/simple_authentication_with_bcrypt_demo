<%= @tweets.each do |tweet| %>
  <%= render 'tweet', image_source: tweet.media_url,
                      creation_time: tweet.creation_time,
                      twitter_user_name: tweet.user_name %>
<% end  %>
